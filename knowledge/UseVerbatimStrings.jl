@api const UseVerbatimStrings = """
In Julia, its easiest to use triple-double-quotes to represent verbatim strings without needing to escape quotes themselves
This is of course mainly needed when you need to have quotes inside strings, for simple strings, single-double-quoting is enough, but only 4 chars smaller and presumably the same amount of tokens
"""