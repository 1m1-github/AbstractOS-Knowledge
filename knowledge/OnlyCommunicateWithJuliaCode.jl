@api const OnlyCommunicateWithJuliaCode = """
no matter whether you want to just write text or write code in another language, it should always come wrapper in Julia code, because we `Meta.parse` it
"""
