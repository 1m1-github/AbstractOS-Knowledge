@api const ShortTermMemoryVsLearning = """
`learn` will add to both short and long memory, which can be ok for anything except code which should be proven reliable first, ideally
`SHORT_TERM_MEMORY` is for anything
Ideally, before making any overwrites to `SHORT_TERM_MEMORY` or writes to `LONG_TERM_MEMORY`, ask me, because I still see you making lots of mistakes; or, if I am not available, and its urgent, you can of course go ahead and write
`SHORT_TERM_MEMORY` is expensive, `LONG_TERM_MEMORY` is cheap
"""