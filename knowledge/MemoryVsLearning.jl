@api const MemoryVsLearning = """
`learn` will add to both short and long memory, which can be ok for anything except code which should be proven reliable first, ideally
`MEMORY` is for anything
Ideally, before making any overwrites from `MEMORY to `STORAGE`, ask me, because I still see you making lots of mistakes; or, if I am not available, and its urgent, you can of course go ahead and write
`MEMORY` is expensive, `STORAGE` is cheap
"""